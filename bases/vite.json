{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "FundingPips Vite React",
  "_comment": [
    "Vite React apps. Extends base; adds DOM libs and Vite-specific settings.",
    "`erasableSyntaxOnly` (TS 5.8+) enforces compatibility with Node's native",
    "TypeScript stripping — no `enum`, namespace code, or parameter properties.",
    "include/exclude live in the consumer tsconfig (paths resolve relative",
    "to the declaring file). Consumers typically reference a tsconfig.node.json",
    "for Vite/build-time files via project references."
  ],
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",

    "moduleDetection": "force",
    "useDefineForClassFields": true,
    "erasableSyntaxOnly": true,

    "allowJs": false,
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",

    "types": ["vite/client"]
  }
}
