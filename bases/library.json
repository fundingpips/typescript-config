{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "FundingPips Library",
  "_comment": [
    "For publishable TS/React libraries. Extends base and adds emit/declaration settings.",
    "include/exclude must be declared in the consumer tsconfig — TS resolves",
    "those paths relative to the declaring file, so a shared package can't",
    "ship them. outDir/rootDir/declarationDir are compiler options and ARE",
    "safe to ship (TS resolves them relative to the consumer's tsconfig)."
  ],
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",

    "noEmit": false,
    "declaration": true,
    "declarationMap": true,
    "declarationDir": "./dist/types",
    "outDir": "./dist",
    "rootDir": "./src",
    "stripInternal": true,
    "composite": true,

    "allowImportingTsExtensions": false,
    "types": []
  }
}
