{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "FundingPips Library",
  "_comment": [
    "For publishable TS/React libraries. Extends base and adds emit/declaration settings.",
    "include/exclude must be declared in the consumer tsconfig — TS resolves",
    "those paths relative to the declaring file, so a shared package can't",
    "ship them. Other path-valued options use ${configDir} so they resolve",
    "against the consumer tsconfig."
  ],
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",

    "noEmit": false,
    "declaration": true,
    "declarationMap": true,
    "declarationDir": "${configDir}/dist/types",
    "outDir": "${configDir}/dist",
    "rootDir": "${configDir}/src",
    "stripInternal": true,
    "composite": true,

    "allowImportingTsExtensions": false,
    "types": []
  }
}
