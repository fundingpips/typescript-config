{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "FundingPips Next.js",
  "_comment": [
    "Only declares compilerOptions that differ from ./base.json.",
    "include/exclude deliberately NOT declared: TS resolves those paths",
    "relative to the file they're declared in, so shipping them from a",
    "node_modules location is always a bug. Consumers must declare their",
    "own include/exclude in their project tsconfig."
  ],
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "esnext"],
    "jsx": "preserve",
    "plugins": [{ "name": "next" }],
    "allowJs": false,
    "checkJs": false,
    "allowImportingTsExtensions": false
  }
}
