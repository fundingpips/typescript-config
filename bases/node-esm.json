{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "FundingPips Node.js (ESM)",
  "_comment": [
    "ESM Node.js services targeting Node 24+. Extends base; overrides module",
    "resolution to NodeNext for import assertions and node: protocol support.",
    "The `ts-node` config block was removed — Node 24 runs TypeScript natively",
    "via --experimental-strip-types (stable in Node 23.6+).",
    "include/exclude live in the consumer tsconfig (paths resolve relative",
    "to the declaring file)."
  ],
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2024"],
    "module": "NodeNext",
    "moduleResolution": "NodeNext",

    "noEmit": false,
    "declaration": true,
    "declarationMap": true,
    "removeComments": true,
    "outDir": "./dist",
    "rootDir": "./src",

    "types": ["node"]
  }
}
